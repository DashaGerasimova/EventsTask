<h3><%= t'.edit_event_message' %></h3>

<%= render 'form', event: @event %> 
<%= link_to t('.delete_button'), @event, method: :delete, data: { confirm: t('confirm')}, class:"expanded alert button" %>
<%= link_to t('Back'), events_path, class: "expanded hollow button" %>
